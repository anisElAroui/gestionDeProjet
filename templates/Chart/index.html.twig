{% extends 'base.html.twig' %}

{% block breadCrumb %}
    {% include 'breadCrumb.html.twig' %}
{% endblock %}

{% block main_content %}
    <div class="container">
        <div class="row">
            <div class="box">
                <div class="col-lg-12">
                    {% trans %}<h1 class="heading">Charts</h1>{% endtrans %}
                    <div class="row">
                        {{ render(controller('App\\Controller\\ChartController::pieAction')) }}
                    </div>
                    {{ render(controller('App\\Controller\\ChartController::colAction')) }}
                    {{ render(controller('App\\Controller\\ChartController::columnAction')) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}