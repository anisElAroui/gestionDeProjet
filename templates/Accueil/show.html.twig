{% extends 'base.html.twig' %}

{% block main_content %}

    <div class="container">
        <div class="row">
            <div class="box">
                <div class="col-lg-12">
                  <h1>Project</h1>

    <div class="table-responsive">
    <table class="table">
        <tbody>
            <tr>
                <th>projectName</th>
                <td>{{ charter.projectName }}</td>
            </tr>
            <tr>
                <th>projectManager</th>
                <td>{{ charter.projectManager }}</td>
            </tr>
            <tr>
                <th>year</th>
                <td></td>
                {#<td>{{ charter.year|date('Y-m-d') }}</td>#}
            </tr>
            <tr>
                <th>realStartDate</th>
                <td></td>
                {#<td>{{ charter.realStartDate |date('Y-m-d') }}</td>#}
            </tr>
            <tr>
                <th>plannedEndDate</th>
                <td></td>
                {#<td>{{ charter.plannedEndDate |date('Y-m-d')}}</td>#}
            </tr>
            <tr>
                <th>budget</th>
                <td>{{ charter.budget }}</td>
            </tr>
            <tr>
                <th>incomes</th>
                <td></td>
                {#<td>{{ charter.incomes }}</td>#}
            </tr>
            <tr>
                <th>Expenses</th>
                <td>{{ charter.plannedExpensesBudget }}</td>
            </tr>
            <tr>
                <th>done</th>
                <td></td>
                {#<td>{{ charter.done }}</td>#}
            </tr>
            <tr>
                <th>endDate</th>
                <td></td>
                {#<td>{{ charter.endDate|date('Y-m-d') }}</td>#}
            </tr>
        </tbody>
    </table>

    </div>

            <br>
            <a class="btn btn-primary" href="{{ path('project_edit', { 'id': charter.id }) }}"><i class="glyphicon glyphicon-edit"></i> edit</a>
            <a class="btn btn-warning" href="{{ path('project_index') }}"><i class="glyphicon glyphicon-arrow-left"></i> Back to the list</a>
            <br><br>
                    {{ form_start(delete_form) }}
                    <input class="btn btn-danger" type="submit" value="Delete">
                    {{ form_end(delete_form) }}
                 </div>
            </div>
        </div>
    </div>

{% endblock %}
