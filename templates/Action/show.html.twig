{% extends 'base.html.twig' %}

{% block body %}


    <div class="container">
        <div class="row">
            <div class="box">
                <div class="col-lg-12">
                  <h1>Action</h1>

    <div class="table-responsive">
    <table class="table">
        <tbody>
            <tr>
                <th>actionType</th>
                <td>{{ action.actionType }}</td>
            </tr>
            <tr>
                <th>priority</th>
                <td>{{ action.priority }}</td>
            </tr>
            <tr>
                <th>initialDate</th>
                <td>{{ action.initialDate |date('Y-m-d') }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ action.author}}</td>
            </tr>
            <tr>
                <th>status</th>
                <td>{{ action.status}}</td>
            </tr>
            <tr>
                <th>currentDate</th>
                <td>{{ action.currentDate |date('Y-m-d') }}</td>
            </tr>
            <tr>
                <th>realDate</th>
                <td>{{ action.realDate |date('Y-m-d') }}</td>
            </tr>
            <tr>
                <th>identificator</th>
                <td>{{ action.identificator}}</td>
            </tr>
        </tbody>
    </table>           

    </div>
            
            <br>
            <a class="btn btn-primary" href="{{ path('finance_edit', { 'id': action.id }) }}"><i class="glyphicon glyphicon-edit"></i> edit</a>
            <a class="btn btn-warning" href="{{ path('finance_index') }}"><i class="glyphicon glyphicon-arrow-left"></i> Back to the list</a>
            <br><br>
                    {{ form_start(delete_form) }}
                    <input class="btn btn-danger" type="submit" value="Delete">
                    {{ form_end(delete_form) }}
                 </div>
            </div>
        </div>
    </div>
            
{% endblock %}
