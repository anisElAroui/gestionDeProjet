{% extends 'base.html.twig' %}

{% block main_content %}


    <div class="container">
        <div class="row">
            <div class="box">
                <div class="col-lg-12">
                  <h1>Finance</h1>

    <div class="table-responsive">
    <table class="table">
        <tbody>
            <tr>
                <th>budget</th>
                <td>{{ finance.budget }}</td>
            </tr>
            <tr>
                <th>sellingPrice</th>
                <td>{{ finance.sellingPrice }}</td>
            </tr>
            <tr>
                <th>Deadline</th>
                <td>{{ finance.Deadline|date('Y-m-d') }}</td>
            </tr>
            <tr>
                <th>expensesPlanned</th>
                <td>{{ finance.expensesPlanned }}</td>
            </tr>
        </tbody>
    </table>           

    </div>
            
            <br>
            <a class="btn btn-primary" href="{{ path('finance_edit', { 'id': finance.id }) }}"><i class="glyphicon glyphicon-edit"></i> edit</a>
            <a class="btn btn-warning" href="{{ path('finance_index') }}"><i class="glyphicon glyphicon-arrow-left"></i> Back to the list</a>
            <br><br>
                    {{ form_start(delete_form) }}
                    <input class="btn btn-danger" type="submit" value="Delete">
                    {{ form_end(delete_form) }}
                 </div>
            </div>
        </div>
    </div>
            
{% endblock %}
